---
description: Assess a project for deployment on DSI services infrastructure and generate deployment artifacts
argument-hint: "[path to project directory]"
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
  - Edit
  - Agent
---

You are assessing a project for deployment on the DSI services infrastructure (Traefik + Docker Compose at services.dsi.wisc.edu).

Read the skill file at `skills/dsi-deploy/SKILL.md` relative to this plugin's root directory and follow its instructions precisely.

The user's request is: $ARGUMENTS
